# 川口市バドミントン予約システム — 使用说明

面向日常运维与部署。功能点只写概要；细节以代码与测试为准。

---

## 1. 功能概要

| 需求 | 入口脚本 | 做什么 |
|------|----------|--------|
| **需求1** 空场扫描 + 邮件 | `scripts/scan_daily.py` | 扫描当月～下月（月末 15 点后扩到下下月）可用时段；变化或每日首次强制发信；邮件含直近予定 / 予約可能 / 予約済み / **予約件数**（件数读 CFG_C4） |
| **需求2** 抽选候补数 | `scripts/lottery_scan.py` | 扫描「当前月+2」抽选候补人数；有变化才发信 |
| **需求4** 自动预约 | `scripts/auto_book.py` | Playwright 按任务自动登录并预约；发结果邮件 |
| **需求5** 预约同步 + 月度统计 | `scripts/sync_calendar.py` | 每天同步团体预约到 Google Calendar（CFG_C2/C3），并重建 CFG_C4 月度统计 |

辅助脚本：`scripts/check_calendar.py`（测日历连通）、`scripts/clear_calendars_once.py`（一次性清空日历，慎用）。

---

## 2. 环境准备

```bash
python -m venv .venv
# Windows: .venv\Scripts\activate
pip install -r requirements.txt
playwright install chromium   # 仅需求4/5 浏览器相关需要

cp .env.example .env
cp local/names.example.json local/names.json
cp local/groups.example.json local/groups.json
cp local/mail_content.example.json local/mail_content.json
# 另需：local/service-account.json、local/venue_short_names.json（简称映射）
```

`local/` 下带密码、凭证、真实团体信息的文件已在 `.gitignore`，**勿提交**。

---

## 3. 环境变量（`.env`）

| 变量 | 用途 |
|------|------|
| `CFG_A1` / `CFG_A2` / `CFG_A3` | SMTP 发件账号 / 专用密码 / 收件人（逗号分隔） |
| `CFG_B1` | 设施预约站点 **HTTP Base URL** |
| `CFG_B2` | 租户路径段（固定 `kawaguchi-city`） |
| `CFG_C1` | Google Calendar **service-account JSON 路径**（如 `local/service-account.json`） |
| `CFG_C2` | 予約用日历 ID（同步写入；邮件「予約済み/直近」也读） |
| `CFG_C3` | 抽選用日历 ID |
| `CFG_C4` | 月度予約統計日历 ID（需求5写入；需求1「予約件数」读取） |
| `CFG_D1` | 可选：`scan_daily` 成功结束后心跳 URL |
| `CFG_D2` | 可选：`sync_calendar` 正常结束后心跳 URL |

日历 Scope：`https://www.googleapis.com/auth/calendar`（读写）。

---

## 4. 输入文件

### 配置（可入库）

| 路径 | 用途 |
|------|------|
| `config/cfg_items.json` | 需求1 扫描场地清单（code / fid / sid） |
| `config/venues.json` | 需求2/4/5 场地元数据（含 `type`: gym/hall） |
| `config/booking_tasks.json` | 需求4 预约任务（day / hours / venue_code / group_ref） |

### 本地私密（gitignore，需自备）

| 路径 | 用途 |
|------|------|
| `.env` | 密钥与日历 ID |
| `local/groups.json` | 团体登录 id/name/password，以及统计用 `type`（gym/hall） |
| `local/names.json` | 场地显示名 |
| `local/mail_content.json` | 邮件 subject/header/footer（`scan` / `lottery` 两段） |
| `local/service-account.json` | Google 服务账号 |
| `local/venue_short_names.json` | 需求5 事件标题用的设施简称 |

---

## 5. 输出文件（`data/`，gitignore）

### 5.1 日志（会增长，有清理）

| 文件 | 写入方 | 是否无限增长 | 清理机制 |
|------|--------|--------------|----------|
| `data/scan.log` | 需求1（及调用 `setup_logging` 的进程） | 会追加 | 启动时 `prune_log`，保留约 **3** 个日历日 |
| `data/booking.log` | 需求4 | 会追加 | 同上，约 **3** 日 |
| `data/lottery.log` | 需求2 | 会追加 | 保留约 **30** 日 |
| `data/changes.log` | 需求1（有变化且发信成功时） | 会追加 | 保留约 **90** 日 |
| `data/cron_stdout.log` | **crontab shell 重定向**（非 Python） | **会无限增长**（若用 `>>`） | 建议改为 **`>` 覆盖**；内容与 `scan.log` 部分重复，仅便于看最近一次 stdout |
| `data/cron_lottery_stdout.log` | 同上（lottery） | 同上 | 建议 **`>` 覆盖**；详细日志看 `lottery.log` |

### 5.2 状态 / 快照（有界或覆盖写）

| 文件 | 写入方 | 是否无限增长 | 说明 |
|------|--------|--------------|------|
| `data/t1.json` | 需求1 | 基本有界 | 当前窗口内 first_seen；消失的 key 会删 |
| `data/t2.json` | 需求1 | 有界 | 上次已发信快照，整文件替换 |
| `data/suppressed.json` | 需求1 | **曾会累积**；现已有过期清理 | 满 4h 静默的 key；**slot 日期早于今天**的会在加载/保存时剔除 |
| `data/fail_count.json` | 需求1 | 有界 | 连续失败计数 `{"n":…}`，成功归零 |
| `data/daily_sent.json` | 需求1 | 有界 | `{"last_sent_date":"YYYY-MM-DD"}` |
| `data/lottery_prev.json` | 需求2 | 有界 | 上次抽选快照，整文件替换 |
| `data/sync_calendar_state.json` | 需求5 | 有界 | 当日 done/attempts/summary_sent；换日逻辑重置 |

### 5.3 外部输出（非本地文件）

- **邮件**：空场报告 / 抽选报告 / 预约结果 / 同步汇总
- **Google Calendar**：C2/C3 预约事件；C4 月度统计（每次同步成功后清空 C4 再按 C2/C3 全量重建当月+下月）
- **心跳**：对 `CFG_D1` / `CFG_D2` 发 GET（未配置则跳过）

---

## 6. 怎么跑

### 需求1 — 空场扫描

```bash
python scripts/scan_daily.py
python scripts/scan_daily.py --force-mail   # 忽略变化门槛，强制发完整快照
```

建议 cron：按小时或按需；stdout 用 **`>`** 覆盖到 `data/cron_stdout.log`。

### 需求2 — 抽选候补

```bash
python scripts/lottery_scan.py
python scripts/lottery_scan.py --no-mail              # 只打印不发信
python scripts/lottery_scan.py --venue-code v03       # 可重复指定场地
```

### 需求4 — 自动预约

```bash
python scripts/auto_book.py                          # 读 booking_tasks.json
python scripts/auto_book.py --headed                 # 显示浏览器
python scripts/auto_book.py --no-wait-9am            # 测试：跳过 9:00 等待
python scripts/auto_book.py --probe                  # 只到时间选择页
python scripts/auto_book.py --venue-code v08 --day 9 --hours 17,19 --group-ref GROUP1
```

**注意**：真实环境不要点到最终「この予約申込を確定する」以外的调试手段若未加 `--stop-after-input` 等开关，会真正提交预约。

### 需求5 — 日历同步

```bash
python scripts/sync_calendar.py                 # 随机选一个未完成团体
python scripts/sync_calendar.py --group-ref GROUP1   # 强制指定团体
```

每日多次调度即可（状态机：`done` 齐或 `attempts ≥ total+2` 后发一次汇总邮件）。成功同步后会更新 CFG_C4。

---

## 7. 模块关系（简图）

```text
设施站点 API ──► scan_daily ──► 邮件(空场+日历板块)
                 lottery_scan ──► 邮件(抽选)

groups.json + 站点 ──► auto_book ──► 邮件(预约结果)

groups.json + 站点 ──► sync_calendar ──► CFG_C2/C3 事件
                                    └──► CFG_C4 统计
                                              ▲
scan_daily 邮件「予約件数」──────────────────┘
```

---

## 8. 测试

```bash
pytest -q
```

---

## 9. 运维注意

1. **crontab 重定向**：用 `>` 不要用 `>>`，避免 `cron_*_stdout.log` 无限膨胀。
2. **CFG_C4**：需求1件数与需求5统计共用；同步会重建 C4，勿在 C4 上手工长期堆放其它重要事件。
3. **groups.json 的 `type`**：只影响 C4 统计正文里体育館/公民館行的团体列表；预约选面仍看 `venues.json` 的 type。
4. **敏感文件**：`.env`、`local/groups.json`、`names.json`、`mail_content.json`、`service-account.json`、`venue_short_names.json` 均勿入库。
5. **心跳**：`CFG_D1`/`CFG_D2` 未配置时安静跳过；失败只打 WARNING，不影响脚本退出码语义（同步单团体失败也会继续走完并心跳）。
